# If you come from bash you might have to change your $PATH.
# export PATH=$HOME/bin:$HOME/.local/bin:/usr/local/bin:$PATH

# Path to your Oh My Zsh installation.
export ZSH="$HOME/.oh-my-zsh"

# Set name of the theme to load --- if set to "random", it will
# load a random theme each time Oh My Zsh is loaded, in which case,
# to know which specific one was loaded, run: echo $RANDOM_THEME
# See https://github.com/ohmyzsh/ohmyzsh/wiki/Themes
ZSH_THEME="" # NOTE: Remove default theme

# Set list of themes to pick from when loading at random
# Setting this variable when ZSH_THEME=random will cause zsh to load
# a theme from this variable instead of looking in $ZSH/themes/
# If set to an empty array, this variable will have no effect.
# ZSH_THEME_RANDOM_CANDIDATES=( "robbyrussell" "agnoster" )

# Uncomment the following line to use case-sensitive completion.
# CASE_SENSITIVE="true"

# Uncomment the following line to use hyphen-insensitive completion.
# Case-sensitive completion must be off. _ and - will be interchangeable.
# HYPHEN_INSENSITIVE="true"

# Uncomment one of the following lines to change the auto-update behavior
# zstyle ':omz:update' mode disabled  # disable automatic updates
# zstyle ':omz:update' mode auto      # update automatically without asking
zstyle ':omz:update' mode reminder # NOTE: just remind me to update when it's time

# Uncomment the following line to change how often to auto-update (in days).
zstyle ':omz:update' frequency 7 # NOTE: Update every 7 days

# Uncomment the following line if pasting URLs and other text is messed up.
# DISABLE_MAGIC_FUNCTIONS="true"

# Uncomment the following line to disable colors in ls.
# DISABLE_LS_COLORS="true"

# Uncomment the following line to disable auto-setting terminal title.
# DISABLE_AUTO_TITLE="true"

# Uncomment the following line to enable command auto-correction.
# ENABLE_CORRECTION="true"

# Uncomment the following line to display red dots whilst waiting for completion.
# You can also set it to another string to have that shown instead of the default red dots.
# e.g. COMPLETION_WAITING_DOTS="%F{yellow}waiting...%f"
# Caution: this setting can cause issues with multiline prompts in zsh < 5.7.1 (see #5765)
# COMPLETION_WAITING_DOTS="true"

# Uncomment the following line if you want to disable marking untracked files
# under VCS as dirty. This makes repository status check for large repositories
# much, much faster.
# DISABLE_UNTRACKED_FILES_DIRTY="true"

# Uncomment the following line if you want to change the command execution time
# stamp shown in the history command output.
# You can set one of the optional three formats:
# "mm/dd/yyyy"|"dd.mm.yyyy"|"yyyy-mm-dd"
# or set a custom format using the strftime function format specifications,
# see 'man strftime' for details.
# HIST_STAMPS="mm/dd/yyyy"

# Would you like to use another custom folder than $ZSH/custom?
ZSH_CUSTOM="$HOME/.zsh" # NOTE: Set custom folder to ~/.zsh

# HACK: Install some plugins if not already installed
{
	CUSTOM_PLUGINS_DIR="${ZSH_CUSTOM:-$HOME/.oh-my-zsh/custom}/plugins"
	download_zsh_plugin() {
		local plugin_name="$1"
		local repo_url="$2"
		local plugin_dir="$CUSTOM_PLUGINS_DIR/$plugin_name"

		if [ ! -d "$plugin_dir" ]; then
			echo "Installing $plugin_name plugin..."
			if git clone --depth=1 "$repo_url" "$plugin_dir"; then
				echo "$plugin_name installed successfully"
			else
				echo "Failed to install $plugin_name"
				return 1
			fi
		fi
	}
	download_zsh_plugin "zsh-autosuggestions" "https://github.com/zsh-users/zsh-autosuggestions"
	download_zsh_plugin "zsh-syntax-highlighting" "https://github.com/zsh-users/zsh-syntax-highlighting.git"
}

# Which plugins would you like to load?
# Standard plugins can be found in $ZSH/plugins/
# Custom plugins may be added to $ZSH_CUSTOM/plugins/
# Example format: plugins=(rails git textmate ruby lighthouse)
# Add wisely, as too many plugins slow down shell startup.
plugins=(
	extract
	ssh
	universalarchive
	# External plugins
	zsh-autosuggestions
	zsh-syntax-highlighting
)

source $ZSH/oh-my-zsh.sh

# User configuration

# Add $HOME/.local/bin to PATH
export PATH=$HOME/.local/bin:$PATH

# Set XDG_CONFIG_HOME to "$HOME/.config" if not already set
if [ -z "$XDG_CONFIG_HOME" ]; then
	export XDG_CONFIG_HOME="$HOME/.config"
fi

# 'bls' a better 'ls' using 'eza' if available
if command -v eza >/dev/null 2>&1; then
	alias bls='eza --color=always --icons=always --group-directories-first'
	alias la='bls -a'
	alias ll='bls -l'
	alias lla='ll -a'
	alias lt='bls -l --tree --level=2 --git'
	alias lta='lt -a'
	alias l='lla'
fi

# nvim-test
alias nvim-test='NVIM_APPNAME=nvim-test nvim'

# Function: Update external plugins_and_themes managed by Oh My Zsh
# Helper function to update Oh My Zsh plugins and themes
omz_update_plugins_and_themes() {
	# Update custom plugins
	if [ -d "$ZSH_CUSTOM/plugins" ]; then
		echo "Checking for custom plugins..."
		for plugin in "$ZSH_CUSTOM/plugins/"*; do
			if [ -d "$plugin/.git" ]; then
				echo "Updating $(basename "$plugin")..."
				git -C "$plugin" pull --rebase
				echo "$(basename "$plugin") update complete!"
			fi
		done
	fi

	# Update custom themes
	if [ -d "$ZSH_CUSTOM/themes" ]; then
		echo "Checking for custom themes..."
		for theme in "$ZSH_CUSTOM/themes/"*; do
			if [ -d "$theme/.git" ]; then
				echo "Updating $(basename "$theme")..."
				git -C "$theme" pull --rebase
				echo "$(basename "$theme") update complete!"
			fi
		done
	fi
}

# Function: update_terminal_info
# Helper function to set terminal information in a remote server
update_terminal_info() {
	# Check if a server argument is provided
	if [[ -z "$1" ]]; then
		echo "Usage: update_terminal_info <server>"
		return 1
	fi

	# Run the command with the provided server argument
	infocmp -x | ssh "$1" -- tic -x -
}

# Zsh-syntax-highlighting
# Tweak colors for zsh-syntax-highlighting
typeset -A ZSH_HIGHLIGHT_STYLES
ZSH_HIGHLIGHT_STYLES[comment]='fg=8'

# Antigravity
if [ -d "$HOME/.antigravity/antigravity/bin" ]; then
	export PATH="$HOME/.antigravity/antigravity/bin:$PATH"
fi

# Windsurf
if [ -d "$HOME/.codeium/windsurf/bin" ]; then
	export PATH="$HOME/.codeium/windsurf/bin:$PATH"
fi

if [ -f "$HOME/.chezmoi/dotfiles/scripts/init/init.zsh" ]; then
	source "$HOME/.chezmoi/dotfiles/scripts/init/init.zsh"
fi
