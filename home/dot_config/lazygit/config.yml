# gui:
#   nerdFontsVersion: "3"
git:
  pagers:
    - pager: delta --dark --paging=never
customCommands:
  - key: "<c-g>"
    command: "git czg"
    description: "Commit with czg"
    context: "files"
    loadingText: "Opening czg"
    output: terminal
